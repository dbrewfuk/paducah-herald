{
  "buildCommand": "php -r \"copy('https://getcomposer.org/installer', 'composer-setup.php');\" && php composer-setup.php --install-dir=/tmp --filename=composer && rm composer-setup.php && /tmp/composer install --no-dev --optimize-autoloader && cp .env.vercel .env && php artisan config:clear && php artisan view:clear",
  "functions": {
    "api/index.php": {
      "runtime": "vercel-php@0.7.2"
    }
  },
  "routes": [
    { "src": "/css/(.*)",    "dest": "/public/css/$1"    },
    { "src": "/js/(.*)",     "dest": "/public/js/$1"     },
    { "src": "/build/(.*)",  "dest": "/public/build/$1"  },
    { "src": "/fonts/(.*)",  "dest": "/public/fonts/$1"  },
    { "src": "/images/(.*)", "dest": "/public/images/$1" },
    { "src": "/favicon.ico", "dest": "/public/favicon.ico" },
    { "src": "/(.*)",        "dest": "/api/index.php"     }
  ]
}
